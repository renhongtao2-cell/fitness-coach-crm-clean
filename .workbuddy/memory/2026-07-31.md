## 国际化改造 + 翻译系统完善

### 10 语言完整翻译
- 原有的 `data.ts` 只有 en-US 和 zh-CN 有真实翻译，其余 8 种语言（ja-JP, ko-KR, es-ES, fr-FR, de-DE, pt-BR, ar-SA, hi-IN）全部指向英文占位符
- 为全部 8 种语言添加了完整翻译（~200+ 个 key），覆盖：common, nav, auth, register, dashboard, coachees, programs, progress, messages, settings, billing, pricing, referral, toast, forms, actions 等所有模块
- 支持语言：英语/中文/日语/韩语/西班牙语/法语/德语/葡萄牙语/阿拉伯语/印地语
- RTL 支持已就绪（ar-SA）

### Bug 修复
- **dashboard/page.tsx**：3 处 `t()` 调用因缺少 `{}` 花括号而被渲染为纯文本字符串
- **coachees/[id]/page.tsx**：11 处 `t()` 调用被当作字符串属性值或 JSX 纯文本，未作为函数调用
- **coachees/page.tsx**：2 处 `t()` 调用在 JSX 三元表达式中被字符串化

### 部署配置
- `deploy.yml`：添加了 `npm ci`（更快）+ Supabase 环境变量
- `vercel.json`：保留 Next.js 框架配置（sing1 区域）
- `.gitignore`：新增 debug/temp 文件排除规则

## 项目全面扫描与修复

- 扫描了整个 FitCoach CRM 项目，发现原项目是从 Next.js 16 全栈应用精简为单文件 SPA
- 识别出 4 个严重问题、6 个中等问题、5 个优化建议
- 全面重写了 `index.html`（从 82 行扩展到约 500 行），修复所有问题：

### 修复内容
- **安全**：虽然仍是 localStorage 原型，但添加了安全警告注释，分离了数据层
- **登录功能**：新增完整的 `page-login` 登录页面
- **addCoachee() 未定义**：实现了完整的添加学员弹窗模态框
- **仪表盘数据**：统计数据现在从 localStorage 动态读取渲染
- **COD 角色跳转 bug**：修复了 `handleRegister` 中传 role 而非 user.type 的问题
- **客户端面板**：新增 `page-client-dashboard`，学员角色现在有独立页面
- **表单验证**：添加邮箱格式、密码长度、姓名长度的前端校验
- **Demo 数据**：教练注册后自动创建 3 个示例学员 + 训练计划 + 消息
- **子导航**：新增页面间 Tab 导航（仪表盘/学员管理/训练计划/消息中心）
- **lang 属性**：从 en 改为 zh-CN
- **SEO**：添加了 description meta

### 项目清理
- 创建了 `.gitignore`（排除 node_modules, .next, .env 等）
- 清理了 `.next/` 残留构建产物（88 个文件）
- 更新了 `deploy.yml`：移除无效的 npm install/build 步骤，改为纯静态部署
- 新增 `vercel.json` 配置静态站点部署
